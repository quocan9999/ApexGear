# Archived SQL Server history

These migration files and standalone SQL exports are historical SQL Server artifacts. They are kept for reading only and must never be applied to the PostgreSQL database.
